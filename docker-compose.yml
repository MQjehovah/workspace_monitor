version: '3.2'

services:
  backend:
    build: ./backend
    # volumes:
    #   - ./backend/bigscreen.db:/app/data/bigscreen.db
    # environment:
    #   - DATABASE_URL=sqlite:///./data/bigscreen.db

  frontend:
    build: ./frontend
    ports:
      - "8092:80"
    depends_on:
      - backend