version: '3.2'

services:
  backend:
    build: ./backend
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/bigscreen.db:/app/bigscreen.db

  frontend:
    build: ./frontend
    ports:
      - "8092:80"
    depends_on:
      - backend

volumes:
  uploads_data:
