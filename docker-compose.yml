version: '3.8'

services:
  backend:
    build: ./backend
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/bigscreen.db

  frontend:
    build: ./frontend
    ports:
      - "8092:80"
    depends_on:
      - backend