version: '3.2'

services:
  backend:
    build: ./backend
    volumes:
      - ./backend/data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "8092:80"
    depends_on:
      - backend

volumes:
  uploads_data:
